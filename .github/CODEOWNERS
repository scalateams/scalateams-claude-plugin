# Default owner for everything in this repo.
# The `main` ruleset requires code-owner review, so this must point at a real
# reviewer (a user or a team that exists). Replace with a team (e.g.
# @scalateams/maintainers) once one is set up.
* @jwgcooke
